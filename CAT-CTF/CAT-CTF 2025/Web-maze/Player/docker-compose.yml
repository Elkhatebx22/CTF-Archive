version: '3.8'

services:
  nginx:
    image: nginx:1.21
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    restart: always

  app:
    build: .
    volumes:
      - .:/app
    restart: always

volumes:
  db_data:
